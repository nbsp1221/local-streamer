# Local Streamer Environment Configuration
# Copy this file to .env if you need to customize settings
# Most settings have sensible defaults in docker-compose.yaml

# ==================================================================
# Optional Customization (uncomment to override defaults)
# ==================================================================

# Change default port (default: 3000)
# PORT=3000

# Adjust resource limits
# MEMORY_LIMIT=1G
# CPU_LIMIT=0.5

# Application settings
# LOG_LEVEL=info

# ==================================================================
# Storage Configuration
# ==================================================================

# Base directory for all application data (default: ./storage)
STORAGE_DIR=./storage

# ==================================================================
# HLS Configuration (for AES-128 encrypted video streaming)
# ==================================================================

# Generate a secure 64-character hex string for encryption seed
HLS_MASTER_ENCRYPTION_SEED=your-64-char-hex-seed-here-replace-with-random-value-123456789
KEY_DERIVATION_ALGORITHM=PBKDF2-SHA256
KEY_DERIVATION_ROUNDS=100000
KEY_SALT_PREFIX=local-streamer-hls-v1

# Generate a secure random string for JWT authentication  
HLS_JWT_SECRET=your-secure-jwt-secret-key-here

# HLS streaming settings
HLS_SEGMENT_DURATION=10
HLS_ENABLED=true
